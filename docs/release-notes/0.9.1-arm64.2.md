# Host Cassettes 0.9.1-arm64.2

## 変更点

- アップデート確認が失敗する不具合を修正
  - `Info.plist` の `SUFeedURL` が旧ユーザー名を参照していたため Sparkle の appcast 取得が 404 エラーになっていた問題を修正

## 動作環境

- macOS 13 Ventura 以降
- Apple Silicon Mac

## インストール

zip を展開して `Host Cassettes.app` をアプリケーションフォルダに移動してください。
