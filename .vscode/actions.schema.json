{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Terminal Actions",
  "description": "Configuration file for the Terminal Actions VS Code extension (.vscode/actions.json).",
  "type": "object",
  "required": ["actions"],
  "properties": {
    "$schema": {
      "type": "string",
      "description": "JSON Schema reference. Do not modify."
    },
    "sections": {
      "type": "array",
      "description": "Explicit display order of sections in the tree view. When omitted, order is derived from the actions array.",
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "commonOnNewTerminalCommand": {
      "type": "string",
      "description": "Optional command that runs once right after a new terminal is created, before action commands. This applies to all actions and is skipped when reusing an existing terminal."
    },
    "newTerminalDelaySeconds": {
      "type": "number",
      "minimum": 0,
      "description": "Optional. Seconds to wait after a new terminal is created, before running commonOnNewTerminalCommand or onNewTerminalCommand. Useful when the editor automatically loads source files on terminal open."
    },
    "actions": {
      "type": "array",
      "description": "List of terminal actions.",
      "items": {
        "$ref": "#/$defs/Action"
      }
    }
  },
  "additionalProperties": false,
  "$defs": {
    "Action": {
      "type": "object",
      "description": "A single terminal action.",
      "required": ["id", "section", "name", "commands"],
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier (auto-generated by the extension). Do not modify manually.",
          "minLength": 1
        },
        "section": {
          "type": "string",
          "description": "Section/group name shown as a collapsible tree branch.",
          "minLength": 1
        },
        "name": {
          "type": "string",
          "description": "Display name of the action shown in the tree view.",
          "minLength": 1
        },
        "commands": {
          "type": "array",
          "minItems": 1,
          "description": "Shell commands to execute in order. Execution stops when a command exits with a non-zero status. If you want to continue after an expected failure, make that command return success using syntax appropriate for the selected shell (for example, `|| true` in sh/bash/zsh). The standard UI currently edits and displays only the first entry.",
          "items": {
            "type": "string",
            "minLength": 1
          }
        },
        "onNewTerminalCommand": {
          "type": "string",
          "description": "Optional command that runs once right after a new terminal is created, before this action's command. This is skipped when reusing an existing terminal."
        },
        "terminalProfile": {
          "type": "string",
          "description": "Terminal profile name to use (e.g. \"bash\", \"zsh\", \"PowerShell\"). When omitted, the default profile is used."
        },
        "reuseTerminal": {
          "type": "boolean",
          "description": "When true (default), reuses an existing terminal for this section. When false, a new terminal is created each time."
        },
        "cwd": {
          "type": "string",
          "description": "Working directory for the command. Supports ${workspaceFolder} substitution."
        },
        "description": {
          "type": "string",
          "description": "Optional human-readable description shown as subtext in the tree view."
        },
        "variables": {
          "type": "array",
          "description": "Variable definitions for command placeholders. Each variable is resolved interactively before the command runs.",
          "items": {
            "$ref": "#/$defs/ActionVariable"
          }
        },
        "confirmBeforeRun": {
          "type": "boolean",
          "description": "When true, shows a confirmation dialog before running the action."
        }
      },
      "additionalProperties": false
    },
    "ActionVariable": {
      "type": "object",
      "required": ["name"],
      "description": "A variable that is resolved interactively before the command runs.",
      "properties": {
        "name": {
          "type": "string",
          "description": "Variable name used in command placeholders (e.g. ${target}).",
          "minLength": 1
        },
        "options": {
          "type": "array",
          "description": "Predefined options shown in a quick-pick dropdown. When omitted or empty, free-text input is used.",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    }
  }
}
